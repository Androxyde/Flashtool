#!SHELLPATH

touch DEVICEWORKDIR/recoveryboot
touch /cache/recovery/boot
cp /system/recovery/RECOV_VERSION/recovery.desc DEVICEWORKDIR/recovery

rm DEVICEWORKDIR/runscript && rm DEVICEWORKDIR/sysrun && /system/bin/reboot